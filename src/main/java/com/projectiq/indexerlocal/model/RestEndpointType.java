package com.projectiq.indexerlocal.model;

/**
 * Enum representing HTTP method types for REST endpoints.
 */
public enum RestEndpointType {
    GET,
    POST,
    PUT,
    DELETE,
    PATCH,
    HEAD,
    OPTIONS,
    UNKNOWN
}